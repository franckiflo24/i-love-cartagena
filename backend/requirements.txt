fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.5.0
python-dotenv==1.2.2
python-jose==3.5.0
python-multipart==0.0.24
pydantic==2.12.5
httpx==0.28.1
bcrypt==4.1.3
passlib==1.7.4
stripe==15.0.1
# Pin below 1.0: anthropic 1.x dropped `temperature` from messages.create()
# (moved to output_config), which TypeError'd EVERY llm_complete call → None →
# Luna silently served her _fallback_response. The unpinned >=0.40.0 floated to
# 1.1.0 and broke the concierge. 0.x keeps the classic API the whole codebase
# uses (llm.py, ai_agent, ai_user_profile, ai_itinerary, demand). 1.x migration
# is a separate, deliberate task. (Aug 2026)
anthropic>=0.40.0,<1.0
google-auth>=2.29.0
pywebpush==2.0.3
